PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Add Hostinger MySQL/MariaDB details when the database is ready.
# If DB_HOST is empty, the app uses local JSON files for development.
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=u380633007_reports
